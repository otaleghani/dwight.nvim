-- dwight/docs.lua
-- ⚠️ RENAMED: This module has been renamed to dwight.skill_fetch to clarify its purpose.
-- It fetches URLs and converts them into skills, not general documentation.
-- This shim forwards all calls for backwards compatibility.

return require("dwight.skill_fetch")
